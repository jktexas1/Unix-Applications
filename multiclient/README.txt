Compile Instructions:
	-There is a makefile, so type "make" to create server and client
	 programs.

Run Instructions:
	server: "./server port yes/no priority.txt"
	-to terminate, use "ctrl c" command.

	client:
	-if no input file:
		"./client servername port yes/no user
	-if input file:
		"./client servername port yes/no user input.txt
		
		